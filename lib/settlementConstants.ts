/** Standardværdier for pantebrevet til søskendene. */

/** Rente på pantebrevet pr. år. */
export const DEFAULT_NOTE_RATE = 0.04;

/** Afdragsperiode på pantebrevet i år. */
export const DEFAULT_NOTE_YEARS = 10;

/** Årlig inflation, når beløbene vises i dagens kroner. */
export const DEFAULT_INFLATION_RATE = 0.03;

/** Tilladt inflation pr. år. */
export const MIN_INFLATION_RATE = 0;
export const MAX_INFLATION_RATE = 0.1;
